# Python Environment

Always use the project's virtual environment when running Python commands.

- Use `.venv/bin/python` to run Python scripts
- Use `.venv/bin/pytest` to run tests
- Use `.venv/bin/pip` to install packages
- Never use bare `python`, `pytest`, or `pip` commands without the `.venv/bin/` prefix
